# Contracts

Homes for correlation / enricher docs (Platform + Integrations):

- `jsm-case-correlation-v0` — Case enums ≠ `jsm.status_map`; ticket ref + map
- `context-enricher-graph-v0` — Graph READ sensor; fail-open+alert; poll (ADR-001)

Add files here as peers publish; do not conflate Case status with JSM workflow status names.
